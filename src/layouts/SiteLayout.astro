---
import { ClientRouter } from "astro:transitions";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { siteConfig } from "../config";
import "../styles/global.css";

interface Props {
  title: string;
  description: string;
  pageClass?: string;
  contentClass?: string;
}

const { title, description, pageClass = "", contentClass = "" } = Astro.props;
const baseUrl = import.meta.env.BASE_URL.endsWith("/")
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href={`${baseUrl}favicon.svg`} />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;1,100;1,200;1,300;1,400;1,500;1,600;1,700&family=Noto+Sans+Arabic:wght@400;500;600;700;800;900&family=Noto+Sans+KR:wght@400;500;600;700;800;900&display=swap"
      rel="stylesheet"
    />
    <ClientRouter />
    <title>{title} - {siteConfig.name}</title>
  </head>
  <body class={`site-liquid ${pageClass}`.trim()}>
    <div class="site-bg" aria-hidden="true">
      <div class="site-bg-layer site-bg-layer-a"></div>
      <div class="site-bg-layer site-bg-layer-b"></div>
      <div class="site-bg-layer site-bg-layer-c"></div>
      <div class="site-bg-grid"></div>
    </div>
    <Header />
    <main class={`page-shell pt-20 ${contentClass}`.trim()}>
      <slot />
    </main>
    <Footer />
  </body>
</html>
